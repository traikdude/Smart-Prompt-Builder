# 🎉 Release v2.15.0 - Smart-Prompt-Builder

## 📅 Release Information
- Date: Monday, April 13, 2026 at 11:59 PM EST
- Tag: Smart-Prompt-Builder-v2.15.0
- Branch: main

## 📦 Included Artifacts
- `App.tsx` & React specific frontend (v2.15.0)
- `backend/main.py` FastAPI logic updates (v2.15.0)
- `backend/scripts/verify_tabs.py`
- `backend/scripts/export_tuning_dataset.py` updates

## 📋 Summary
Deployment of **Phase 7.5 (Telemetry & Version Tagging)** integration. The RLHF Ideal-Output Feedback Pipeline is completely functional. Both the frontend web-app and the backend now globally track their version context via `APP_VERSION = "2.15.0"`. Real-world upvotes and downvote-corrections are independently routed across targeted Google Sheet sub-tabs (`Telemetry` vs `Corrections`).

## 🔄 Changes in This Release

### ✨ Added
- Added global `APP_VERSION = "2.15.0"` configuration array synchronized across `package.json`, `main.py`, and `App.tsx`.
- Implemented telemetry script workflows including `backend/scripts/verify_tabs.py` and dual-path RLHF fine-tuning export configuration in `backend/scripts/export_tuning_dataset.py`.
- Finalized feedback capture UI logic providing dual-axis capture and validation context.

### 🔧 Changed
- Routing configuration updated in backend `main.py` API: It seamlessly silos 1-rating interactions mapping them to `Telemetry` and 0-rating interactions to `Corrections`.
- Overrode default frontend state constants effectively decoupling logic and pushing version attributes via telemetry APIs simultaneously. 

### 🐛 Fixed
- Resolved browser/agent DOM coordinate-selection stability bugs by switching automated metrics evaluation pipelines explicitly back via native python verification scripts.

## 🧪 Testing Performed
- End-to-end feedback iteration simulation.
- Successful manual upvote generation validation into remote `Telemetry` storage.
- Substantive downvote corrections input successfully logged inside Google Admin `Corrections` storage.
- Verified Secret Manager credential flow (`GOOGLE_SERVICE_ACCOUNT_JSON` & `RLHF_SHEET_ID`) locally.

## 📝 Notes
This release successfully caps off Phase 7.4 & 7.5 RLHF infrastructure enabling self-improving prompt generation using curated outputs.

## 🔗 Links
- Tag: `releases/tag/Smart-Prompt-Builder-v2.15.0`
